package treeDataStructures;

public class SelfBalancingTree {
	/*
	 * if We know the keys In advanced, we can make a perfectly balanced 
	 * binary Search Tree(BST).
	 * Main idea is to keep the height as O(log n) for which rotation 
	 * need to be done.
	 * Q-> How to keep it balanced when random insertion/deletion happening?
	 * 
	 * Note:left rotation:antiClock wise
	 * right rotation:Clockwise
	 */

}
